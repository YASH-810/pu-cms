# Development Utilities

These scripts are local inspection and repair helpers used while stabilizing the CMS database and APIs.

They are not part of production startup or deployment. Prefer the package scripts in `backend/package.json` for normal setup, migrations, tests, and seed tasks.
